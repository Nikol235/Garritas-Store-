server:
  port: 8000

spring:
  application:
    name: garritas-gateway
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://localhost:18888"
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true